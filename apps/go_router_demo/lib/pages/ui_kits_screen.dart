import 'package:flutter/material.dart';

class UIKitsScreen extends StatelessWidget {
  const UIKitsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('UIKits Screen'),
    );
  }
}
